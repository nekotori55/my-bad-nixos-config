{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Switch to config",
            "type": "shell",
            "command": "sudo nixos-rebuild switch",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}